#ifndef ANIMATE_H
#define ANIMATE_H
unsigned int w=0;
//~ void handle_logic(){
//~
  //~ if(camera[0].old_x!=map[0].backDim.x){
  //~ camera[0].decalage=-camera[0].old_x+map[0].backDim.x;
  //~ for(unsigned int e=0;e<=enemy.size();e++){
  //~ enemy[e].ins.x-=camera[0].decalage;
//~ }
//~ for(unsigned int e=0;e<=enemy.size();e++){
//~
  //~ giant[e].ins.x-=camera[0].decalage;
//~ }
  //~ camera[0].old_x=map[0].backDim.x;
  //~ }
//~ }
//~ if(!enemy.empty()){
  //~ if(w<=enemy.size()){
//~ 
    //~ if(enemy[w].walk && !enemy[w].finish ){
      //~ zombie_walk(w);
    //~ }
    //~ else if(enemy[w].attack && !enemy[w].finish ){  zombie_attak(w);}
  //~ w++}else{w=0;}
//~ }

#endif
